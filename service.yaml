apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80       # Port exposed inside the cluster
      targetPort: 80 # Port the container listens on
      nodePort: 30007 # Port exposed on EC2 host
